SEED: 32
n_edges: 10000 # all edges to use: 1500000, all: 16223942 :16223942
n_epo: 1
k_folds: 5
batch_size: 128
num_classes: 5
gene_dim: 40
hidden_dim: 128
learning_rate: 0.0001
test_size: 0.33
scale_features: "0,1,3"
n_neighbors: 20
min_dist: 0.8
cutoff_pval: 5e-2
metric: "correlation"
p_base: ../data/
p_significant_edges: ${p_base}significant_gene_relations.tsv
p_seed_features: ${p_base}seed_features.tsv
p_out_links: ${p_base}out_links_aml.csv
p_out_genes: ${p_base}out_genes_aml.csv
p_out_gene_rankings: ${p_base}out_gene_rankings_aml.csv
p_merged_signals: ${p_base}merged_signals_aml.csv
p_nedbit_features: ${p_base}nedbit_features_aml.csv
p_dnam_features: ${p_base}dnam_features_aml.csv
p_nedbit_dnam_features: ${p_base}df_nebit_dnam_features_aml.csv
p_nedbit_dnam_features_norm: ${p_base}df_nebit_dnam_features_norm.csv
p_combined_pos_neg_signals: ${p_base}combined_pos_neg_signals_aml.csv
p_test_probe_genes: ${p_base}test_probe_genes_aml.csv
p_train_probe_genes: ${p_base}train_probe_genes_aml.csv
p_pred_likely_pos: ${p_base}pred_likely_pos_aml.csv
p_pred_likely_pos_no_training_genes_probes: ${p_base}pred_likely_pos_no_training_genes_probes_aml.csv
p_torch_data: ${p_base}data_aml.pt
p_torch_embed: ${p_base}embed_conv_aml.pt
p_torch_embed_batch_norm: ${p_base}embed_batch_norm_aml.pt
p_true_labels: ${p_base}true_labels_aml.pt
p_torch_model: ${p_base}model_aml.pt
p_pred_labels: ${p_base}pred_labels_aml.pt
p_pred_probs: ${p_base}pred_probs_aml.pt
p_plot: ${p_base}
p_data: ${p_base}
p_model: ../model/
exp_epo: 5

# Preprocess AML Illumina arrays
# Input files
p_arrays: ${p_base}GSE175758_GEO_processed.txt # Path to arrays matrix (TSV).
p_mapper: ${p_base}GPL13534-11288-mapper-HMBC450.txt # Path to probe-gene mapper (TSV with columns: probeID, UCSC_RefGene_Name).
p_snp_probes: ${p_base}snp_7998probes.vh20151030.txt # Path to SNP probe IDs (CSV single column).
p_seeds_methylated_cpgs: ${p_base}non-randomly-demethylated-CpGs.tsv # Path to seed probes (TSV with columns: probeID, deltaBeta, P.Value, CHR).
p_seeds_gene_methylation_expr: ${p_base}genes_symbol_methylation_expression.tsv # Path to gene methylation-expression file (CSV with columns: Gene, Methylation, Expression).
# Parameters
size_negative: 10000 # Number of negative samples to draw
corr_threshold: 0.33 #0.5 # Correlation threshold for gene-gene links

# Parameters for assigning pre-labels
nedbit_header: 1 # data header 0/1
quantile_1: 0.05 # quantile threshold 1
quantile_2: 0.2 # quantile threshold 2

# Preprocessed data path
download_raw_data: true # true: downloads raw data from Zenodo using the link from 'p_raw_data'
p_raw_data: https://zenodo.org/records/17194431/files/aml_raw_datasets.zip?download=1

use_preprocessed_data: false # true: downloads preprocessed data from Zenodo using the link from 'p_processed_data'
p_processed_data: https://zenodo.org/records/17193720/files/aml_processed_datasets_c_0.33_te_10000.zip?download=1
